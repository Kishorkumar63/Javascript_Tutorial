 
 //Stream  is read && write data is called as Stream

 // Image file is Comming   Req data is Very Large 

 //Buffer is   athu oru kurpeta memory  
 //buffer is temporary storage
 //buffer can be stored data  temporary 
// Buffer can be it take some part data in requesting




//on() this method can be listen requseting(req) data come or not 
// req.on()=> this method can be send event (data come and data send )


//req.on("data",(chunk)=>{console.log(chunk)   })
